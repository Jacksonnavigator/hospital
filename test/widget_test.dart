import 'package:flutter_test/flutter_test.dart';
import 'package:qmedco/main.dart';

void main() {
  test('QmedCO app widget can be created', () {
    expect(const QmedCOApp(), isA<QmedCOApp>());
  });
}
